import { describe, expect, jest, test } from '@jest/globals';
import loadUpcomingScreenings from '../server/loadUpcomingScreenings.js';

describe("loadUpcomingScreenings()", () => {

    test("no empty properties", async () => {
        const res = await loadUpcomingScreenings()

    })











})